 if($item['usuario'] == $usuariopassado){
                echo  "<script>alert('Email enviado com Sucesso!');</script>";
};

CASE SENSITIVEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEE

O CAMPO DO POST É PASSADO PELO name DO FORMMMMMM


 $db =  new PDO('mysql:host=localhost;dbname=projeto;charset=utf8', 'root', 'root');
        $consulta = $db->query('SELECT usuario, senha FROM cadastros');
        $listaconsulta = $consulta->fetchAll();
        $retorno = null;
    
        foreach($listaconsulta as $item){
            if($item['usuario'] == $_POST['email'] && $item['senha'] == $_POST['senha']){
                header("location:paginalogada.php");
                break;
            } else {
                header("location:index2.php");
                echo "<script>alert('E-mail ou senha inválidos')</script>";
            };
        };
        
   


                            <?php if(isset($_GET['email']) && $_GET['email']==1){ ?>

                                <small id="senha-informativo" class="form-text text-danger text-center">Usuário ou senha incorretos. Esqueceu a senha? <a href="#">Clique aqui para recuperar</a></small>
                                
                            <?php } else { ?> 

                                <small id="senha-informativo" class="form-text text-muted">Troque de senha regularmente para garantir sua segurança.</small> 

                            <?php } ?>


    <?php if(isset($_GET['defineusurario']) && $_GET['defineusurario']==1){ ?>

    <script type="text/javascript">
        $(window).on('load',function(){
        $('#modalcadastro').modal('show'); });
    </script>




 <?php if ($_GET['valor']==1){ ?>

                                <small id="senha-informativo" class="form-text text-danger text-center">Usuário ou senha incorretos. Esqueceu a senha? <a href="#">Clique aqui para recuperar</a></small>
                                
                            <?php } else { ?> 

                                <small id="senha-informativo" class="form-text text-muted">Troque de senha regularmente para garantir sua segurança.</small> 

                            <?php } ?>